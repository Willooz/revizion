import Vue from 'vue'
import App from './App.vue'

document.addEventListener("DOMContentLoaded", function(event) {
  // console.log('Hello Revizion!')
  new Vue({
    el: 'body',
    components: { App }
  })
})
